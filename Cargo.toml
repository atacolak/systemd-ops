[package]
name = "systemd-mcpd"
version = "0.1.0"
edition = "2021"
description = "A Model Context Protocol server for systemd. Read-only, capability-scoped."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
strip = true
