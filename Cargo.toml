[package]
name = "systemd-mcpd"
version = "0.3.0"
edition = "2021"
description = "A Model Context Protocol server for systemd. Capability-scoped; reads by default, writes only through plan/apply."
license = "MIT"
repository = "https://github.com/rhaist/systemd-mcpd"
readme = "README.md"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
strip = true
